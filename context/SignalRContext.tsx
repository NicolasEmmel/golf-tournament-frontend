"use client";

import type { ReactNode } from "react";

/**
 * Phase 4: shared SignalR connection and typed hub actions.
 * Foundation stub — passes children through until hub integration is implemented.
 */
export function SignalRProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
