"use client";

import type { ReactNode } from "react";

/**
 * Phase 4+: shared tournament day and leaderboard snapshots.
 * Foundation stub — passes children through until REST/SignalR sync is implemented.
 */
export function TournamentProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
